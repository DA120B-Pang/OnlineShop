package zaar.product.Menu;

import javafx.scene.layout.VBox;

/**
 * Use in subclasses in classes that need menuactions :) make sense?
 */
public interface MenuAction {
    public void action(Menus menu);
}
